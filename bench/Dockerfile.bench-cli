# Bench image form with the Docker CLI in it. Use it to test
# the Sentinel Docker-CLI call path with the socket present.
FROM sentinel-bench
RUN apt-get update \
 && apt-get install -y --no-install-recommends docker.io \
 && rm -rf /var/lib/apt/lists/*
